server:
  port: 8084

spring:
  application:
    name: course-creation-service
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/course_creation_db?createDatabaseIfNotExist=true
    username: root
    password: shubhs#R83
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true